spring.application.name=krishi-shetra-backend
server.port=8080

# MongoDB
spring.data.mongodb.uri=mongodb+srv://okdb_user:ok_db123@cluster0.9xpltlm.mongodb.net/krishi_shetra?appName=Cluster0
spring.data.mongodb.database=krishi_shetra

# JWT
app.jwt.secret=krishi-shetra-super-secret-jwt-key-2026-sih-hackathon
app.jwt.expiration-ms=604800000

# File upload (for product images stored as base64 in DB)
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# CORS - allow Next.js dev server
app.cors.allowed-origins=http://localhost:3000,http://localhost:3001

# Logging
logging.level.com.krishishetra=DEBUG
